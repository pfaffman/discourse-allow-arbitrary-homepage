# Set Home page to arbitrary URL

This theme component will allow an admin to specify the default homepage to any URL.

See `home url override` in the theme settings.
# discourse-allow-arbitrary-homepage
